# Vanishing Tic-Tac-Toe

Игра "Крестики-нолики с исчезновением" - многопользовательская онлайн-игра с уникальными правилами.

## Правила игры

- Поле 3x3, как в стандартных крестиках-ноликах
- Игра для двух игроков в режиме реального времени
- **Особенность:** Правило исчезновения символов
  - Каждый игрок может иметь **максимум 3 символа** на доске одновременно
  - При размещении 4-го символа **самый старый символ игрока исчезает**
  - Символы противника НЕ затрагиваются
  - **Визуальная подсказка:** клетка, которая исчезнет, помечена ⏳ и подсвечена оранжевым

## Структура проекта (монорепо)

```
vanishing_tic_tac_toe/
├── backend/          # FastAPI + WebSocket
├── frontend/         # React + TypeScript
├── docker-compose.yml           # Для локальной разработки
├── docker-compose.prod.yml      # Для продакшн с Traefik
└── README.md
```

## Технологии

### Backend
- Python 3.12+
- FastAPI
- WebSocket для реального времени
- Чистая архитектура (SOLID принципы)

### Frontend
- React 18
- TypeScript
- WebSocket client
- Modern UI/UX

### Инфраструктура
- Docker & Docker Compose
- Traefik 3.5 (reverse proxy для продакшн)
- Nginx (для статики frontend)

## Быстрый старт

### Предварительные требования

- Docker и Docker Compose
- Для продакшн: доменное имя и Cloudflare аккаунт
- Для локальной разработки backend: [UV](https://github.com/astral-sh/uv) (опционально)

### Локальная разработка

```bash
# Клонировать репозиторий
git clone <your-repo>
cd vanishing_tic_tac_toe

# Запустить все сервисы через Docker
make dev

# Или в фоновом режиме
make dev-d
```

Приложение будет доступно:
- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

### Продакшн деплой

**Два варианта:**
- 🚀 **Быстрый деплой** с Let's Encrypt: [DEPLOYMENT_SIMPLIFIED.md](DEPLOYMENT_SIMPLIFIED.md) 
- 📚 **Подробное руководство**: [DEPLOYMENT.md](DEPLOYMENT.md)

Краткая версия:
```bash
# 1. Настроить окружение
cp env.prod.example .env.prod
nano .env.prod  # Заполнить переменные

# 2. Создать Docker network
docker network create web

# 3. Настроить SSL
touch traefik/acme.json
chmod 600 traefik/acme.json

# 4. Запустить
make prod
```

## API Endpoints

- `GET /` - Health check
- `GET /api/health` - Backend health status
- `WS /ws/{player_id}` - WebSocket connection для игры

## Разработка

### Backend (локально без Docker)

```bash
cd backend

# Установить UV если не установлен
# curl -LsSf https://astral.sh/uv/install.sh | sh

# Установить зависимости
uv sync

# Запустить сервер
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Frontend (локально без Docker)

```bash
cd frontend

# Установить зависимости
npm install

# Создать .env файл (опционально)
echo "REACT_APP_WS_URL=localhost:8000" > .env

# Запустить dev сервер
npm start
```

### Полезные команды (Make)

```bash
make help              # Показать все доступные команды
make dev               # Запустить dev окружение
make prod              # Запустить продакшн
make stop              # Остановить контейнеры
make logs              # Просмотр логов
make clean             # Очистка (удалить контейнеры и volumes)
```

## Архитектура

Проект следует принципам SOLID и чистой архитектуры:

- **Backend**: Разделение на слои (models, services, api)
- **Frontend**: Компонентная архитектура с разделением логики
- **WebSocket**: Реал-тайм коммуникация для синхронизации игры

## Contributing

Хотите внести свой вклад? Прочитайте [CONTRIBUTING.md](CONTRIBUTING.md) для получения инструкций.

## Структура файлов

```
vanishing_tic_tac_toe/
├── backend/                 # FastAPI backend
│   ├── app/
│   │   ├── models/         # Игровые модели (Domain)
│   │   ├── services/       # Бизнес-логика (Application)
│   │   ├── websocket/      # WebSocket handlers
│   │   └── main.py         # FastAPI entry point
│   ├── pyproject.toml      # UV dependencies
│   └── Dockerfile
│
├── frontend/                # React frontend
│   ├── public/
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   ├── hooks/          # Custom hooks
│   │   ├── types/          # TypeScript типы
│   │   └── App.tsx
│   ├── package.json
│   └── Dockerfile
│
├── traefik/                 # Traefik конфигурация
│   ├── traefik.yml         # Статическая конфигурация
│   └── config.yml          # Динамическая конфигурация
│
├── scripts/                 # Вспомогательные скрипты
│   ├── setup-dev.sh
│   ├── setup-prod.sh
│   └── backup.sh
│
├── docker-compose.yml       # Локальная разработка
├── docker-compose.prod.yml  # Продакшн с Traefik
├── Makefile                 # Удобные команды
├── DEPLOYMENT.md            # Инструкции по деплою
└── README.md
```

## Roadmap

### Планируемые функции

- [ ] Система рейтингов игроков
- [ ] История игр (с PostgreSQL)
- [ ] Профили пользователей
- [ ] Чат между игроками
- [ ] Звуковые эффекты
- [ ] Темная тема
- [ ] Мобильное приложение (React Native)
- [ ] Турнирный режим
- [ ] Достижения и статистика

## Известные проблемы

- WebSocket переподключение может занять несколько секунд
- В Safari могут быть проблемы с WebSocket (в работе)

## Troubleshooting

### Backend не запускается

```bash
# Проверить логи
make logs-backend

# Пересобрать контейнер
docker-compose build backend
docker-compose up -d backend
```

### Frontend не подключается к backend

1. Проверьте переменную `REACT_APP_WS_URL` в `.env`
2. Убедитесь, что backend запущен на порту 8000
3. Проверьте CORS настройки в `backend/app/main.py`

### SSL сертификаты не работают (продакшн)

1. Проверьте DNS записи
2. Убедитесь, что Cloudflare proxy отключен
3. Проверьте API токен Cloudflare
4. Посмотрите логи Traefik: `make logs-traefik`

## FAQ

**Q: Нужна ли база данных?**  
A: Нет, для базовой версии игры база данных не требуется. Все данные хранятся в памяти. Если хотите сохранять историю игр, можно добавить PostgreSQL.

**Q: Сколько игроков могут играть одновременно?**  
A: Технически нет ограничений. Backend может обрабатывать множество одновременных игр.

**Q: Можно ли играть против бота?**  
A: В текущей версии нет. Это можно добавить как будущую функцию.

**Q: Как масштабировать на несколько серверов?**  
A: Для этого потребуется добавить Redis для синхронизации состояния между инстансами и настроить балансировку нагрузки через Traefik.

## Благодарности

- FastAPI - отличный веб-фреймворк для Python
- React - мощная библиотека для UI
- Traefik - современный reverse proxy
- UV - быстрый менеджер пакетов Python

## Лицензия

MIT License - см. [LICENSE](LICENSE)

