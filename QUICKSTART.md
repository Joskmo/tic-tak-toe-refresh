# Quick Start Guide

Быстрый старт для разработки и тестирования игры "Крестики-нолики с исчезновением".

## 🚀 Локальный запуск (5 минут)

### Вариант 1: Docker (рекомендуется)

Самый простой способ - использовать Docker:

```bash
# 1. Клонировать репозиторий
git clone <your-repo-url>
cd vanishing_tic_tac_toe

# 2. Запустить всё одной командой
make dev

# Или если make не установлен:
docker-compose up --build
```

✅ **Готово!** Откройте http://localhost:3000 в двух вкладках браузера и начните играть!

### Вариант 2: Локальная разработка без Docker

#### Backend

```bash
cd backend

# Установить UV (если еще не установлен)
# Windows (PowerShell):
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# Linux/macOS:
# curl -LsSf https://astral.sh/uv/install.sh | sh

# Установить зависимости и запустить
uv sync
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend (в отдельном терминале)

```bash
cd frontend

# Установить Node.js зависимости
npm install

# Запустить dev сервер
npm start
```

✅ **Готово!** Откройте http://localhost:3000

## 🎮 Как играть

1. **Откройте игру в двух вкладках/окнах браузера**
2. В каждой вкладке нажмите **"Найти игру"**
3. Игроки автоматически соединятся
4. Играйте по очереди!

### Правила

- 🎯 Цель: выстроить 3 символа в ряд (горизонталь, вертикаль, диагональ)
- 🔄 Игроки ходят по очереди (X и O)
- ✨ **Особенность**: Правило исчезновения!
  - Каждый игрок может иметь максимум 3 символа на доске
  - При 4-м ходе игрока его самый старый символ исчезает
  - Клетка, которая исчезнет следующей, помечена ⏳ и подсвечена оранжевым

## 🛠️ Полезные команды

```bash
# Просмотр логов
make logs

# Остановить сервисы
make stop

# Полная очистка
make clean

# Логи backend
make logs-backend

# Логи frontend
make logs-frontend
```

## 🔍 API и Документация

### Swagger UI (Backend API)

Откройте http://localhost:8000/docs для интерактивной документации API.

### Endpoints

- `GET /` - Health check
- `GET /api/health` - Detailed health
- `WS /ws/{player_id}` - WebSocket для игры

## 🐛 Troubleshooting

### Backend не запускается

```bash
# Проверить логи
docker logs vanishing_ttt_backend

# Пересобрать
docker-compose build backend
docker-compose up -d backend
```

### Frontend не может подключиться

Проверьте, что backend запущен:
```bash
curl http://localhost:8000/api/health
```

Если не работает - проверьте переменную окружения:
```bash
# frontend/.env должен содержать:
REACT_APP_WS_URL=localhost:8000
```

### Порт занят

Измените порты в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # backend
  - "3001:80"    # frontend
```

## 📝 Разработка

### Структура проекта

```
backend/
├── app/
│   ├── models/      # Игровая логика
│   ├── services/    # Бизнес-логика
│   ├── websocket/   # WebSocket handlers
│   └── main.py      # FastAPI app

frontend/
├── src/
│   ├── components/  # React компоненты
│   ├── hooks/       # Custom hooks (WebSocket)
│   └── types/       # TypeScript типы
```

### Запуск тестов

```bash
# Backend tests
cd backend
uv run pytest

# Frontend tests (когда добавите)
cd frontend
npm test
```

### Hot Reload

- Backend: автоматически перезагружается при изменении `.py` файлов
- Frontend: автоматически обновляется при изменении в `src/`

## 🚀 Следующие шаги

1. **Изучите код**: начните с `backend/app/models/game.py`
2. **Добавьте функции**: см. [CONTRIBUTING.md](CONTRIBUTING.md)
3. **Деплой**: следуйте [DEPLOYMENT.md](DEPLOYMENT.md)

## 💡 Советы

- Используйте VS Code с рекомендованными расширениями (`.vscode/extensions.json`)
- Для отладки backend используйте VS Code debugger (F5)
- Откройте Chrome DevTools → Network → WS для просмотра WebSocket сообщений

## 🆘 Нужна помощь?

- 📖 Полная документация: [README.md](README.md)
- 🚢 Деплой: [DEPLOYMENT.md](DEPLOYMENT.md)
- 🤝 Contributing: [CONTRIBUTING.md](CONTRIBUTING.md)
- 🐛 Issues: Create an issue on GitHub

---

**Приятной разработки! 🎮**

