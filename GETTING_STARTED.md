# Getting Started - Первый запуск проекта

Пошаговое руководство для первого запуска проекта "Vanishing Tic-Tac-Toe".

## 🎯 Выберите ваш сценарий

### 🏠 Сценарий 1: Локальное тестирование (Windows)

Вы хотите быстро запустить и протестировать игру на своем компьютере.

**Требования:**
- Docker Desktop установлен и запущен
- Git установлен

**Шаги:**

1. **Откройте PowerShell или CMD**

2. **Клонируйте проект (если еще не клонирован)**
   ```powershell
   # Уже сделано, вы находитесь в папке проекта
   cd S:\Coding\Python\vanishing_tic_tac_toe
   ```

3. **Запустите Docker Desktop** (если не запущен)

4. **Запустите проект одной командой**
   ```powershell
   docker-compose up --build
   ```

5. **Откройте браузер**
   - Откройте http://localhost:3000 в ДВУХ вкладках браузера
   - В каждой вкладке нажмите "Найти игру"
   - Играйте!

**Остановка:**
```powershell
# Нажмите Ctrl+C в PowerShell
# Или в новом окне:
docker-compose down
```

### 💻 Сценарий 2: Разработка без Docker

Вы разработчик и хотите запускать backend и frontend локально для разработки.

#### Backend (в первом терминале)

```powershell
cd backend

# Установить UV (если не установлен)
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"

# Перезапустить PowerShell после установки UV

# Установить зависимости
uv sync

# Запустить backend
uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend (во втором терминале)

```powershell
cd frontend

# Установить Node.js зависимости
npm install

# Создать .env файл
echo REACT_APP_WS_URL=localhost:8000 > .env

# Запустить frontend
npm start
```

**Готово!** Браузер откроется автоматически на http://localhost:3000

### 🚀 Сценарий 3: Деплой на сервер

Вы хотите развернуть приложение на публичном сервере.

**Требования:**
- Сервер Linux (Ubuntu/Debian рекомендуется)
- Docker установлен на сервере
- Домен настроен на ваш сервер
- Cloudflare аккаунт (для SSL)

**Подробное руководство:** См. [DEPLOYMENT.md](DEPLOYMENT.md)

**Краткие шаги:**

```bash
# На сервере

# 1. Клонировать
git clone <your-repo>
cd vanishing_tic_tac_toe

# 2. Настроить окружение
cp env.prod.example .env.prod
nano .env.prod  # Заполнить DOMAIN, пароли, API токены

# 3. Запустить setup скрипт
chmod +x scripts/setup-prod.sh
./scripts/setup-prod.sh
```

## 📝 Быстрые команды

### Docker Compose команды

```powershell
# Запустить (foreground)
docker-compose up

# Запустить (background)
docker-compose up -d

# Остановить
docker-compose down

# Просмотр логов
docker-compose logs -f

# Пересобрать
docker-compose build

# Полная очистка (удалить всё)
docker-compose down -v
```

### Makefile команды (если установлен make)

```bash
make dev        # Запустить dev окружение
make stop       # Остановить
make logs       # Просмотр логов
make clean      # Очистить всё
make help       # Показать все команды
```

**Установить make на Windows:**
```powershell
# Через Chocolatey
choco install make

# Или через scoop
scoop install make
```

## 🎮 Как играть

1. **Откройте две вкладки браузера** на http://localhost:3000
2. **В каждой вкладке нажмите "Найти игру"**
3. **Игроки автоматически соединятся**
4. **Играйте по очереди!**

### Правила

- Поле 3×3, стандартные правила крестиков-ноликов
- Цель: выстроить 3 символа в ряд
- **Уникальное правило**: Каждый 3-й ход самый первый ход исчезает!
  - Ход 1: X ставит на [0,0]
  - Ход 2: O ставит на [0,1]
  - Ход 3: X ставит на [1,0] → клетка [0,0] очищается!

## 🔍 Проверка работоспособности

### Backend

```powershell
# Проверить health
curl http://localhost:8000/api/health

# Или откройте в браузере
start http://localhost:8000/docs
```

### Frontend

```powershell
# Откройте в браузере
start http://localhost:3000
```

### Docker контейнеры

```powershell
# Список запущенных контейнеров
docker ps

# Вы должны увидеть:
# - vanishing_ttt_backend
# - vanishing_ttt_frontend
```

## 🐛 Решение проблем

### Docker не запускается

```powershell
# Проверить статус Docker Desktop
docker version

# Перезапустить Docker Desktop
# Или через PowerShell (от администратора):
Restart-Service docker
```

### Порт занят

Если порт 8000 или 3000 занят:

1. Найдите процесс:
   ```powershell
   netstat -ano | findstr :8000
   netstat -ano | findstr :3000
   ```

2. Остановите процесс или измените порт в `docker-compose.yml`

### Frontend не подключается к backend

1. Проверьте, что backend запущен:
   ```powershell
   curl http://localhost:8000/api/health
   ```

2. Проверьте переменную окружения в `frontend/.env`:
   ```
   REACT_APP_WS_URL=localhost:8000
   ```

### UV не устанавливается на Windows

Используйте альтернативный метод:

```powershell
# Скачать напрямую
curl -LsSf https://github.com/astral-sh/uv/releases/latest/download/uv-installer.ps1 | powershell

# Или установить через pip
pip install uv
```

## 📚 Дополнительные ресурсы

- **Быстрый старт**: [QUICKSTART.md](QUICKSTART.md)
- **Полная документация**: [README.md](README.md)
- **Деплой на сервер**: [DEPLOYMENT.md](DEPLOYMENT.md)
- **Разработка**: [CONTRIBUTING.md](CONTRIBUTING.md)
- **Настройка окружения**: [ENV_SETUP.md](ENV_SETUP.md)
- **Обзор проекта**: [PROJECT_SUMMARY.md](PROJECT_SUMMARY.md)

## 🆘 Поддержка

Если что-то не работает:

1. Проверьте [TROUBLESHOOTING секцию в README.md](README.md#troubleshooting)
2. Посмотрите логи: `docker-compose logs -f`
3. Создайте issue на GitHub с описанием проблемы

## ✅ Checklist первого запуска

- [ ] Docker Desktop установлен и запущен
- [ ] Git установлен
- [ ] Проект клонирован
- [ ] Запущена команда `docker-compose up`
- [ ] Backend доступен на http://localhost:8000
- [ ] Frontend доступен на http://localhost:3000
- [ ] Открыты две вкладки браузера
- [ ] Игра работает!

---

**Приятной игры! 🎮**

Если всё работает, переходите к изучению кода или попробуйте добавить новые функции!

